{
  "name": "baseline_tree_soare",
  "description": "Plain SFT on tree search with entropy's opener. Controlled comparison against baseline_entropy: same opener, so only the policy differs.",
  "seed": 20260817,
  "dedup": false,
  "shuffle": true,
  "components": [
    {
      "source": "data/tree_soare/train.jsonl",
      "repeat": 1
    }
  ]
}